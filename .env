# Local lab credentials, deliberately fake and only reachable on your machine.
# Committed so `docker compose up` works on a fresh clone with nothing to set up.
# One copy of each value: the db container and the API's DSN both read from here.
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppassword
POSTGRES_DB=appdb
